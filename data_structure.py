# 자료구죠
# 자료들을 저장하는 여러가지 구조

# 시퀀스
# 요소로 구성
# 요소 간에는 순서가 있음
# 시퀀스의 요소들은 번호가 붙어 잇음
# 내장 시퀀스(str, bytes, bytearray, list, tuple, rabnge)

# 동일한 연산을 지원
# 인덱싱
# 슬라이싱
# 뎃셈 연산
# 곱셈 연산

# 내장함수 적용가능
# len() - 시퀀스의 길이를 반환
# max() - 최대값
# min() - 최소값